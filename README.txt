Enable the module and check sample htaccess to implement yourself
# Tips:
- Check Recent log messages to identify the issues to get the modules work
- Enable boost_cache_clear so the caches would be auto flushed where there is changes
